<h1>Bugs</h1>

<table>
  <thead>
    <tr>
      <th>Title</th>
      <th>Description</th>
      <th>Developer</th>
      <th colspan="3">Actions</th>
    </tr>
  </thead>

  <tbody>
    <% @bugs.each do |bug| %>
      <tr>
        <td><%= bug.title %></td>
        <td><%= bug.description %></td>
        <td><%= bug.user ? bug.user.email : "Unassigned" %></td>
        <td><%= link_to 'Show', project_bug_path(@project, bug) if can? :read, bug %></td>
        <td><%= link_to 'Edit', edit_project_bug_path(@project, bug) if can? :update, bug %></td>
        <td><%= link_to 'Destroy', project_bug_path(@project, bug), method: :delete, data: { confirm: 'Are you sure?' } if can? :destroy, bug %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<%= link_to 'New Bug', new_project_bug_path(@project) if can? :create, Bug %>
<%= link_to 'Back', project_path(@project) %>
